<Window x:Class="MRTest.InfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MRTest"
        mc:Ignorable="d"
        Title="InfoWindow" Height="150" Width="250"  WindowStyle="None" Opacity="0.9" WindowStartupLocation="CenterScreen" Background="Transparent">
    <Border x:Name="MainWind"  CornerRadius="20" Background="White" MouseDown="MainWind_MouseDown" >

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>

            <Label x:Name="info" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center" Content="Разожмите руку и удерживайте 5 секунд"/>
            <Button x:Name="exit" Grid.Row="1" Content="ok" Style="{StaticResource MenuButton}" HorizontalAlignment="Right" Click="exit_Click"/>
        </Grid>
    </Border>
</Window>
